import { PublicOrder, OrderBookSnapshot, OrderBookDelta } from '../util/types/shared';
/**
 * Internal class for handling OrderBook delta transforms
 */
export declare class OrderBook implements OrderBookSnapshot {
    seqNum: number;
    bids: PublicOrder[];
    asks: PublicOrder[];
    constructor(snapshot?: OrderBookSnapshot);
    getSnapshot(): OrderBookSnapshot;
    applySnapshot(snapshot: OrderBookSnapshot): void;
    applyDelta(delta: OrderBookDelta, ignoreSeqNum?: boolean): OrderBook;
    private applyOrderDeltas;
}
